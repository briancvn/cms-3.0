import { Routes } from '@angular/router';

import { UISandboxComponent } from './Components/UISandboxComponent';

export const ROUTES: Routes = [
    { path: '', component: UISandboxComponent },
    { path: 'ui-sandbox', component: UISandboxComponent },
];

